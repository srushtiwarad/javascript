function convertMinutesToSeconds(minutes) {
    return minutes * 60;
}

// Example from the image:
const minutes = 5;
const result = convertMinutesToSeconds(minutes);

console.log(result); // Output: 300