function findLargest(num1, num2) {
    // condition ? trueValue : falseValue
    return num1 > num2 ? num1 : num2;
}

// Example usage:
console.log(findLargest(4, 7));
console.log(findLargest(10, 2)); 